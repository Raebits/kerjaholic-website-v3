import { ProfileTestimoniModel } from "../../models/profile/profile-testimoni-model"

type TestimonialProfileComponentProps = {
    data: ProfileTestimoniModel,
    widthtDevice?: number,
    horizontal: boolean
}

export default TestimonialProfileComponentProps